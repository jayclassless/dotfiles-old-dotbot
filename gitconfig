[user]
    name = Jason Simeone
    email = jay@classless.net

[github]
    user = jayclassless

[core]
    excludesfile = ~/.gitignore_global
    editor = vi

[color]
    diff = auto
    status = auto
    branch = auto

[push]
    default = simple

[pull]
    rebase = true

[merge]
    tool = vimdiff

[mergetool]
	keepBackup = false

[alias]
    co = checkout
    ci = commit
    st = status
    in = !git fetch && git log ..origin/master
    out = !git fetch && git log origin/master..
    rollback = reset --soft HEAD~

[include]
    path = ~/.gitconfig_local

[rerere]
    enabled = true

